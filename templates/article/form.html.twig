<div class="row">
		<div class="col">
			<p class="titre text-center text-muted">{{ title }}</p>
		</div>
	</div>
	{{ form_start(form) }}
		<div class="row">
			<div class="col-md-4">
				{{ form_row(form.imageFile) }}
			</div>
			<div class="col-md-4">
				{{ form_row(form.designation) }}
			</div>
			<div class="col-md-4">
				{{ form_row(form.quantity_available) }}
			</div>
		</div>
		<div class="row">
			<div class="col-md-4">
				{{ form_row(form.critical_quantity) }}
			</div>
			<div class="col-md-4">
				{{ form_row(form.unit_price) }}
			</div>
			<div class="col-md-4">
				{{ form_row(form.typeArticle) }}
			</div>
		</div>
		{{ form_row(form.isPromo) }}
		<div class="row">
			<div class="col">
				<button type="submit" class="btn btn-primary btn-lg btn-block" formnovalidate>{{btn_txt}}</button>
			</div>
			<div class="col">
				<a href="#" class="btn btn-outline-secondary btn-lg btn-block">Annuler</a>
			</div>
		</div>
	{{ form_end(form) }}