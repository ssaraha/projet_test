{% for type, messages in app.flashes(['success', 'error', 'info', 'warning']) %}
	{% for message in messages %}
        <div class="alert alert-{{type}} msg-alert-success" role="main">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}